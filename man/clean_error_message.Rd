% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/clean_error_message.R
\name{clean_error_message}
\alias{clean_error_message}
\title{Clean and Validate Error Messages}
\usage{
clean_error_message(error_message)
}
\arguments{
\item{error_message}{The input to be processed. This should be an object of
class \code{"error"}. Non-error inputs will result in a blank string being returned.}
}
\value{
A cleaned, user-friendly error message as a string. Returns an empty
string if the input is not an error object.
}
\description{
\code{clean_error_message} processes and cleans error messages to remove unnecessary
details such as ANSI escape codes, CLI-specific bullets, stack traces, and
technical context. It ensures the input is a valid error object before processing
and returns a blank string (\code{""}) if the input is not an error.
}
\examples{
# Processing a Valid Error Message
tryCatch({
  stop("Something went wrong.")
}, error = function(e) {
  cleaned <- clean_error_message(e)
  print(cleaned)
})

# Handling Non-Error Input
cleaned <- clean_error_message("This is not an error object")
print(cleaned)

}
