% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/filter_out_years.R
\name{filter_out_years}
\alias{filter_out_years}
\title{Filter Dataset by Removing Specified Years}
\usage{
filter_out_years(.data, years_to_exclude)
}
\arguments{
\item{.data}{A \code{cd_data} object, which contains a dataset with DHIS-2 indicators
and demographic data organized by year.}

\item{years_to_exclude}{An integer vector that specifies the years to exclude
from the dataset.}
}
\value{
A \code{cd_data} object with rows filtered to exclude those matching
the specified years in \code{years_to_exclude}. The resulting data set is intended
for focused analysis across the remaining years.
}
\description{
The \code{filter_out_years} function is used to exclude specified years from a DHIS-2
dataset, refining the data for more targeted analysis or reporting. This is
particularly useful when focusing on specific timeframes, such as recent years
only or a custom-defined analysis period. By removing data from unwanted years,
you can create a tailored dataset aligned with analytical goals.
}
\details{
\itemize{
\item \strong{Purpose}: Use this function when specific years are unnecessary for
analysis, such as baseline years or years with incomplete data.
\item \strong{Validation}:
\itemize{
\item Ensures \code{.data} is a valid \code{cd_data} object for DHIS-2 analysis.
\item Confirms \code{years_to_exclude} is a vector of integers; raises an error if
not properly specified.
}
\item \strong{Functionality}: Filters \code{.data} to retain only the rows where the \code{year}
does not match any value in \code{years_to_exclude}.
}
}
\examples{
\dontrun{
  # Example: Remove data for 2017 and 2018 from a DHIS-2 dataset
  filtered_data <- filter_out_years(dhis_data, years_to_exclude = c(2017, 2018))
}

}
