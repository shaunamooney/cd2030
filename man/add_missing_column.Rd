% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils-quality_checks.R
\name{add_missing_column}
\alias{add_missing_column}
\title{Add Missing Value Flags to Health Indicators}
\usage{
add_missing_column(.data, indicators)
}
\arguments{
\item{.data}{A data frame containing health indicator data.}

\item{indicators}{A character vector specifying the names of the indicator
columns to analyze for outliers.}
}
\value{
A data frame containing:
\itemize{
\item \strong{Missingness Flags}:
For each indicator, a column prefixed with \code{mis_} indicating missingness (\code{1} for
missing, \code{0} otherwise).
\item \code{district} and \code{year} columns to maintain grouping information.
}
}
\description{
\code{add_missing_column} assesses data quality by identifying missing values for
specified health indicators. It generates new columns for each indicator,
flagging values as 'Missing' (\code{1}) or 'Non-Missing' (\code{0}) to support downstream
quality control and analysis.
}
\details{
\itemize{
\item \strong{Missingness Assessment}:
Each indicator is processed to flag missing values (\code{NA}) with \code{1} (missing)
or \code{0} (non-missing). The resulting columns are prefixed with \code{mis_} followed
by the original indicator name (e.g., \code{mis_indicator1}).
\item \strong{Group Preservation}:
The output retains the \code{district} and \code{year} columns to ensure grouping information
remains intact.
}
}
\examples{
\dontrun{
  # dd missing value flags for all indicators
  add_missing_column(data)
}

}
