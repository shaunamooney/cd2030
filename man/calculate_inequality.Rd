% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/4a_equity_calculate_inequality.R
\name{calculate_inequality}
\alias{calculate_inequality}
\title{Analyze Subnational Health Coverage Data with Inequality Metrics}
\usage{
calculate_inequality(
  .data,
  admin_level = c("adminlevel_1", "district"),
  un_estimates,
  sbr = 0.02,
  nmr = 0.025,
  pnmr = 0.024,
  anc1survey = 0.98,
  dpt1survey = 0.97,
  twin = 0.015,
  preg_loss = 0.03
)
}
\arguments{
\item{.data}{A data frame containing subnational health coverage data.}

\item{admin_level}{A character string specifying the administrative level for analysis.
Options: \code{"adminlevel_1"} (e.g., regions) or \code{"district"}.}

\item{un_estimates}{(Optional) A data frame with UN population estimates for national
population-level calculations. Required if using population-based metrics.}

\item{sbr}{Numeric. The stillbirth rate (default: 0.02).}

\item{nmr}{Numeric. The neonatal mortality rate (default: 0.025).}

\item{pnmr}{Numeric. The post-neonatal mortality rate (default: 0.024).}

\item{anc1survey}{Numeric. Survey-based ANC-1 coverage rate (default: 0.98).}

\item{dpt1survey}{Numeric. Survey-based Penta-1 coverage rate (default: 0.97).}

\item{twin}{Numeric. The twin birth rate (default: 0.015).}

\item{preg_loss}{Numeric. The pregnancy loss rate (default: 0.03).}
}
\value{
A tibble (\code{cd_inequality} object) containing:
\itemize{
\item Subnational health coverage metrics.
\item Population shares.
\item MADM, MRDM, and related disparity metrics.
}
}
\description{
\code{calculate_inequality} computes subnational health coverage metrics and evaluates
disparities compared to national averages. The function provides metrics such as:
}
\details{
\itemize{
\item \strong{Mean Absolute Difference to the Mean (MADM)}: Average absolute deviation
from the national mean.
\item \strong{Weighted MADM}: MADM weighted by population share.
\item \strong{Mean Relative Difference to the Mean (MRDM)}: MADM as a percentage of
the national mean.
\item \strong{Weighted MRDM}: MRDM weighted by population share.
\item \strong{Relative Difference Max (RD Max)}: Adjusted maximum difference metric.
}

The function allows analysis of specific health indicators at subnational levels
(\code{adminlevel_1} or \code{district}) and compares them with national-level data.
}
\examples{
\dontrun{
  # Example analysis for district-level data
  inequality_metrics <- calculate_inequality(
    .data = health_data,
    admin_level = "district",
    un_estimates = un_data
  )
}

}
