% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/1c_adjust_generate_adjustment_values.R
\name{generate_adjustment_values}
\alias{generate_adjustment_values}
\title{Generate Adjusted and Unadjusted Service Counts by Year}
\usage{
generate_adjustment_values(
  .data,
  adjustment = c("default", "custom", "none"),
  k_factors = NULL
)
}
\arguments{
\item{.data}{A \code{cd_data} dataframe containing service data for adjustments.}

\item{adjustment}{A character string specifying the adjustment type:
\itemize{
\item \code{"default"}: Applies preset k-factors (e.g., 0.25) for each indicator group.
\item \code{"custom"}: Uses user-specified \code{k_factors} values for each indicator group.
\item \code{"none"}: Skips adjustments, returning only the raw data.
}}

\item{k_factors}{A named numeric vector of custom k-factor values between 0 and 1 for
each indicator group (e.g., \code{c(anc = 0.3, idelv = 0.2, ...)}). Required if
\code{adjustment = "custom"}.}
}
\value{
A \code{cd_adjustment_values} tibble containing:
\itemize{
\item Columns for unadjusted values, suffixed with \verb{_raw}.
\item Columns for adjusted values, suffixed with \verb{_adj}.
\item A \code{year} column indicating the year of each count.
}
}
\description{
\code{generate_adjustment_values} calculates yearly unadjusted and adjusted service counts
for each indicator within a \code{cd_data} dataset, applying the specified adjustment type.
It provides a comparison of raw and adjusted values for analysis purposes.
}
\details{
This function performs the following steps:
\enumerate{
\item \strong{Data Validation}: Ensures \code{.data} is of the \code{cd_data} class and \code{adjustment}
is correctly specified.
\item \strong{Unadjusted Summation}: Calculates the yearly sums of unadjusted service counts.
\item \strong{Adjusted Summation}: Applies \code{\link[=adjust_service_data]{adjust_service_data()}} to compute adjusted values,
then calculates the yearly sums.
\item \strong{Combining Results}: Merges unadjusted and adjusted yearly counts for comparison.
}
}
\examples{
\dontrun{
# Generate adjustment values with default k-factors
adjustment_values_default <- generate_adjustment_values(data, adjustment = "default")

# Generate adjustment values with custom k-factors
custom_k <- c(anc = 0.3, idelv = 0.2, pnc = 0.35, vacc = 0.4, opd = 0.3, ipd = 0.25)
adjustment_values_custom <-
  generate_adjustment_values(data, adjustment = "custom", k_factors = custom_k)

# Generate unadjusted values only
unadjusted_values <- generate_adjustment_values(data, adjustment = "none")
}

}
\seealso{
\code{\link[=adjust_service_data]{adjust_service_data()}} for the detailed adjustment function.
}
